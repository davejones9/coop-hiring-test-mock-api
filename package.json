{
  "name": "simple-json-server",
  "version": "1.0.0",
  "description": "Returns a single JSON object with simple properties for a test.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}
